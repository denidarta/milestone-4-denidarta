$kind: http-example
request:
  url: "{{baseUrl}}/api/v1/accounts?page=string&limit=string&userId=string"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    page: string
    limit: string
    userId: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
