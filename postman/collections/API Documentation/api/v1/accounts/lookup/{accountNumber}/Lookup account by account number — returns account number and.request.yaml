$kind: http-request
name: Lookup account by account number — returns account number and owner name
url: "{{baseUrl}}/api/v1/accounts/lookup/:accountNumber"
method: GET
pathVariables:
  accountNumber: "9046.563391912932"
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Lookup account by account number — returns account number
  and.resources/examples
order: 1000
