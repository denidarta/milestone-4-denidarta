$kind: http-example
request:
  url: "{{baseUrl}}/api/v1/accounts/:accountId/transactions?page=string&limit=str\
    ing"
  method: GET
  headers:
    - key: Authorization
      value: Bearer <token>
      description: "Added as a part of security scheme: bearer"
  queryParams:
    page: string
    limit: string
  pathVariables:
    accountId: "9046.563391912932"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
