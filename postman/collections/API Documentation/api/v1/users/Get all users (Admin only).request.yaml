$kind: http-request
url: "{{baseUrl}}/api/v1/users"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get all users (Admin only).resources/examples
order: 4000
