$kind: http-request
url: "{{baseUrl}}/api/v1/users/me"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Get current logged in user.resources/examples
order: 1000
